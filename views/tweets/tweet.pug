
extends ../layout.pug
block content


  // Includes an external JavaScript file
  script(src="/javascripts/tweets/tweet.js")
  
  // Creates a flex container with row direction
  div(class="d-flex flex-row content mx-auto")
    // Includes the profile.pug template
    include ../includes/profile.pug

    // Creates a flex container with column direction
    div(class="flex-fill d-flex flex-column" id="tweet-list-container")
        // Includes the tweet-list.pug template
        include tweet-list.pug
